# Repository Guidelines & Rules

## Git & Commit Rules
- **Never use `--no-verify`**: All git commits must execute and pass pre-commit hooks, linters, and formatters. Never bypass git hook verification flags.
